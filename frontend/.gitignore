/node_modules
/dist
/.vite
/.cache
/.env
/.env.local
*.log
*.bak
*.swp
.DS_Store
Thumbs.db
/coverage
/.idea
/.vscode
# Vitest writes a throwaway ESM copy of the TS config next to it and does not
# always clean it up. Left behind, `eslint .` picks it up and fails the
# `--max-warnings 0` gate on an unrelated `no-undef`.
*.timestamp-*.mjs